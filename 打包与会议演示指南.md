# 打包与会议演示指南

更新时间：2026-06-25

本文档用于会议现场演示 MultiMaterialSlicer。它包含当前已经验证过的 macOS 打包结果、Windows 打包方式、演示步骤、测试 STL 文件和注意事项。

## 一、当前交付包

当前 macOS 打包产物位置：

```text
/Users/asher/Library/Mobile Documents/com~apple~CloudDocs/Documents/3D-GUI/dist/MultiMaterialSlicer-mac-x86_64.zip
```

这个 zip 包内包含：

- `MultiMaterialSlicer.app`
- Qt 5.12.12 运行库
- `machine_material_presets.yaml`
- 后端命令行工具 `slice_merge_tool`
- 开发备用脚本 `slice_1080p.py`

当前包是 x86_64 架构。Intel Mac 可以直接运行；Apple Silicon Mac 需要 Rosetta。

## 二、macOS 重新打包

如果会议前需要重新生成包，在仓库根目录运行：

```bash
cd "/Users/asher/Library/Mobile Documents/com~apple~CloudDocs/Documents/3D-GUI"
bash scripts/package_macos.sh
```

打包成功后会输出：

```text
dist/MultiMaterialSlicer-mac-x86_64.zip
```

推荐使用 `bash scripts/package_macos.sh`，不要直接使用 `./scripts/package_macos.sh`。项目在 iCloud Drive 路径下，直接执行脚本时可能受到文件扩展属性或执行权限影响。

## 三、macOS 打包脚本做了什么

`scripts/package_macos.sh` 会自动完成：

1. 调用 `scripts/package_backend_macos.sh`，用 PyInstaller 把 `slice_1080p.py` 打成 `backend_dist/slice_merge_tool`。
2. 用 CMake 编译 Qt/C++ APP。
3. 把预设库 `config/machine_material_presets.yaml` 复制进 APP 的 `Contents/Resources`。
4. 把后端工具 `slice_merge_tool` 复制进 APP 的 `Contents/Resources`。
5. 调用 `macdeployqt` 打包 Qt 依赖。
6. 清理 iCloud 扩展属性、`.DS_Store` 和 AppleDouble 文件。
7. 对 Qt framework、plugin、主程序和后端工具进行 ad-hoc 签名。
8. 输出 zip 包。

## 四、Windows 打包

Windows 上需要准备：

- Qt 5.12.12，例如 `C:\Qt\5.12.12\msvc2017_64`
- Visual Studio C++ 编译工具
- CMake
- Python

在 PowerShell 中运行：

```powershell
cd "项目目录"
powershell -ExecutionPolicy Bypass -File scripts\package_windows.ps1 -QtPrefix C:\Qt\5.12.12\msvc2017_64
```

脚本默认会先打包后端 `slice_merge_tool.exe`，再编译 GUI，最后调用 `windeployqt.exe`。输出：

```text
dist\MultiMaterialSlicer-win64.zip
```

如果只打包后端：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\package_backend_windows.ps1
```

说明：当前这台机器是 macOS，已经实际打包并验证 macOS 包；Windows 包需要在 Windows 环境实机执行脚本。

## 五、会议现场打开 APP

推荐会议前把 zip 解压到一个本地非 iCloud 临时目录，例如桌面或下载目录。

打开方式：

```bash
open "/path/to/MultiMaterialSlicer.app"
```

如果从终端调试，可以用：

```bash
open -n "/path/to/MultiMaterialSlicer.app"
```

如果 macOS 提示安全拦截，可以在 Finder 中右键 APP，选择“打开”，再确认打开。当前包已做 ad-hoc 签名，但未做 Apple notarization。

## 六、推荐演示 STL

仓库已经准备了测试 STL：

```text
demo_stl/01_basic_block.stl
demo_stl/02_cylinder_20mm.stl
demo_stl/03_star_badge.stl
demo_stl/04_three_towers.stl
demo_stl/multi_A_base_plate.stl
demo_stl/multi_B_cross_insert.stl
```

会议推荐组合：

- `demo_stl/multi_A_base_plate.stl`
- `demo_stl/multi_B_cross_insert.stl`
- 可选：`demo_stl/03_star_badge.stl`

这三个模型适合展示不同材料、模型复制、位置调整和最终合并输出。

## 七、会议演示步骤

### 1. 打开 APP

打开 `MultiMaterialSlicer.app`。确认界面出现：

- 左侧 3D 构建平台。
- 右侧模型列表、变换设置、材料设置。
- 材料数量默认为 3。
- 窗口可以拖拽调整大小。

### 2. 导入 STL

点击“导入 STL”，选择：

```text
demo_stl/multi_A_base_plate.stl
demo_stl/multi_B_cross_insert.stl
```

导入后左侧 3D 区域应显示模型，右侧列表会显示模型名称和材料。

### 3. 修改模型变换

选中一个模型后，在右侧输入：

- X 平移：例如 `8.5`
- Y 平移：例如 `-4`
- Z 旋转：例如 `30`
- 等比例缩放：例如 `1.25`

输入后 3D 视图会更新模型位置、角度和大小。

### 4. 修改材料

选中模型，在材料下拉框中把模型设为材料2或材料3。模型颜色会随材料改变，模型列表也会更新为类似：

```text
multi_B_cross_insert [材料2]
```

### 5. 复制模型

选中模型，点击“复制”。列表中会出现副本，例如：

```text
multi_B_cross_insert 副本 [材料2]
```

复制件会保留原模型的变换和材料，方便现场展示多材料复制逻辑。

### 6. 切换到切片页

切换到“切片/导出”页，确认：

- 材料参数表里显示“底层光强”和“普通光强”。
- 顶部/模型相关切片设置已经放到模型设置下方。
- 后端工具路径指向 APP 内部的 `slice_merge_tool`。

### 7. 设置高级选项

勾选：

- `挡板 (groove)`
- `刮板1 (slide_0)`

可以顺手说明：

- groove 已按会议要求翻译成“挡板”。
- slide_0、slide_1、slide_2 已翻译成“刮板1、刮板2、刮板3”。

### 8. 生成输出

选择输出目录，点击“导出切片并生成 GCode”。

成功后 APP 会弹窗提示：

```text
已生成切片、config.yaml 与 run.gcode。
```

输出目录中应包含：

```text
config.yaml
material_0/
material_1/
material_2/
merged/
```

`merged` 目录中应包含：

```text
config.yaml
run.gcode
若干合并后的 png
```

## 八、已经完成的实际测试

本机已经完成以下验证：

- 重新打包 macOS 自包含 zip。
- 解压最终 zip 后打开 APP。
- 使用 Computer Use 实际操作 GUI。
- 验证默认材料数量为 3。
- 验证窗口可调节大小。
- 验证 3D 视图显示模型。
- 验证模型材料修改后列表和颜色更新。
- 验证平移、旋转、缩放输入可以影响 3D 视图。
- 验证模型复制按钮可用。
- 验证切片页显示“光强”而不是“电流”。
- 验证高级选项显示“挡板”和“刮板1/2/3”。
- 验证点击生成后可以输出 `config.yaml`、各材料 PNG、合并 PNG 和 `run.gcode`。
- 验证后端实际调用的是打包进 APP 的 `slice_merge_tool`。
- 验证 `run.gcode` 中包含 `groove`、`slide 0` 和投影指令。

Computer Use 无法稳定控制 macOS 原生文件选择器，所以自动化测试中有一段使用 APP 启动参数导入 STL。这个路径调用的是同一套 STL 导入逻辑。人工会议演示时直接使用“导入 STL”按钮选择文件即可。

## 九、会议前快速自检命令

如果会议前想用命令行快速确认包没有损坏：

```bash
cd "/Users/asher/Library/Mobile Documents/com~apple~CloudDocs/Documents/3D-GUI"
rm -rf /tmp/mms_check
mkdir -p /tmp/mms_check
ditto -x -k dist/MultiMaterialSlicer-mac-x86_64.zip /tmp/mms_check
codesign --verify --deep --strict --verbose=2 /tmp/mms_check/MultiMaterialSlicer.app
open -W -n /tmp/mms_check/MultiMaterialSlicer.app --args --selftest demo_stl/multi_A_base_plate.stl demo_stl/multi_B_cross_insert.stl
```

如果最后一个命令退出码为 0，说明 APP、后端、配置库和输出链路都可用。

## 十、现场兜底方案

如果现场文件选择器不方便，可以从终端直接带 STL 参数启动：

```bash
open -n "/path/to/MultiMaterialSlicer.app" --args \
  "/Users/asher/Library/Mobile Documents/com~apple~CloudDocs/Documents/3D-GUI/demo_stl/multi_A_base_plate.stl" \
  "/Users/asher/Library/Mobile Documents/com~apple~CloudDocs/Documents/3D-GUI/demo_stl/multi_B_cross_insert.stl"
```

APP 启动后会自动导入这两个模型，然后可以继续在 GUI 中修改材料、复制模型、设置输出目录并生成结果。
